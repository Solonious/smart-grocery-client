import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'smart.grocery.client',
  appName: 'smart-grocery-client',
  webDir: 'dist/smart-grocery-client/browser',
};

export default config;
